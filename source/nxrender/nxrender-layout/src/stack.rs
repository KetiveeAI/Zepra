//! Stack Layout (Z-axis layering)

/// Stack layout - layers widgets on top of each other
#[derive(Debug, Clone, Default)]
pub struct StackLayout;

impl StackLayout {
    pub fn new() -> Self {
        Self
    }
}
