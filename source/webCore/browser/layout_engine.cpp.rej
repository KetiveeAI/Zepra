--- layout_engine.cpp
+++ layout_engine.cpp
@@ -486,6 +498,11 @@
     static thread_local unsigned long lastFrameTime = 0;
     
     if (s_paintCount == 0) {
+        static bool dumped = false;
+        if (!dumped) {
+            dumpLayoutTree(box);
+            dumped = true;
+        }
         lastFrameTime = 0; // Assuming layout reset sets this conceptually, wait handle this externally
     }
     s_paintCount++;
